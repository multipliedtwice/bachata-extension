{
  "version": 1,
  "id": "claude-plan",
  "name": "Implementation plan — one planner (compatibility)",
  "description": "Same planning workflow with a legacy default assignment. For new runs, choose Implementation plan and select Agents.",
  "longitudinalIntent": "runLocal",
  "agents": [
    {
      "id": "claude",
      "name": "Planner",
      "adapter": "claude-code",
      "command": "claude",
      "permissionMode": "plan"
    }
  ],
  "steps": [
    {
      "id": "plan",
      "name": "Implementation plan",
      "enabled": true,
      "humanGate": "none",
      "type": "agent",
      "participants": [
        "claude"
      ],
      "promptTemplate": "Inspect the current repository and produce a precise implementation plan for this request without changing files. State the files you would change, the order of the changes, the risks, and how the result would be verified.\n\n{{userPrompt}}",
      "parallel": false,
      "consensus": false,
      "permissionModes": {
        "claude": "plan"
      },
      "attachments": "selected"
    }
  ]
}
