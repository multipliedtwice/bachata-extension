{
  "version": 1,
  "id": "codex-fix",
  "name": "Fix with Codex",
  "description": "Codex diagnoses and implements the fix inside a declared write scope. Verification is controller-owned, not model-reviewed. No commit is created.",
  "longitudinalIntent": "initiativeRequired",
  "agents": [
    {
      "id": "codex",
      "name": "Codex",
      "adapter": "codex-app-server",
      "command": "codex",
      "permissionMode": "workspaceWrite",
      "approvalPolicy": "onRequest"
    }
  ],
  "steps": [
    {
      "id": "diagnose",
      "name": "Diagnose the defect",
      "enabled": true,
      "humanGate": "none",
      "type": "agent",
      "participants": ["codex"],
      "promptTemplate": "Diagnose the reported defect from the current repository and supplied evidence. Identify the reproducible cause before proposing changes. Do not modify files in this step.\n\n{{userPrompt}}",
      "parallel": false,
      "consensus": false,
      "permissionModes": { "codex": "readOnly" },
      "approvalPolicies": { "codex": "onRequest" },
      "attachments": "selected"
    },
    {
      "id": "fix",
      "name": "Implement the fix",
      "enabled": true,
      "humanGate": "before",
      "type": "agent",
      "participants": ["codex"],
      "promptTemplate": "Implement the root-cause fix you diagnosed. Stay inside the declared write scope. Do not run repository commands: the controller owns verification.\n\nDiagnosis:\n{{peerAnswersTagged}}",
      "parallel": false,
      "consensus": false,
      "permissionModes": { "codex": "workspaceWrite" },
      "approvalPolicies": { "codex": "onRequest" },
      "attachments": "none"
    }
  ],
  "managedPolicy": {
    "writeScope": "workspace",
    "commitMode": "never",
    "verificationChecks": [
      { "id": "workspace-integrity", "command": "bachata:workspace-integrity" },
      { "id": "project-checks", "command": "bachata:project-checks" }
    ]
  }
}
