# Recorded longitudinal runs

One file per task and arm: `<task-id>/<arm>.json`.

Empty on purpose. No round has been recorded for this release. The benchmark makes no
claim about accumulated refinement until a human records rounds here.
