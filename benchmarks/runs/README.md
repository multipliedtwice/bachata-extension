# Recorded runs

One file per task and arm: `<task-id>/<arm>.json`.

Empty on purpose. No run has been recorded for this release. The benchmark makes no
claim about pairing until a human records runs here.
